{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "preferences",
  "description": "Capability for the Scout preferences window",
  "windows": ["preferences"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
